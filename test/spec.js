const { expect } = require('chai');
// const app = require('supertest')(require('../server/api'));


describe('test ', () => { 
  it('returns true ', () => {
    expect(true).to.equal(true);
  });
}); 